# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
out/
.turbo/

# Env / secrets — never commit real values
.env
.env.*
!.env.example

# OS / editor
.DS_Store
*.log
.idea/
.vscode/

# Bot runtime data (SQLite db + downloaded proof photos)
# NOTE: anchored/specific so it never matches source folders named `data`.
/data/
apps/bot/data/
*.db
*.db-wal
*.db-shm

# TypeScript incremental build info
*.tsbuildinfo
